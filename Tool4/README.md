# PredictPath AI - Tool 4: Response Planning

## 📋 Overview
Tool 4 generates automated response plans based on the forecasted trajectories. It suggests firewall rules, user isolation steps, and patch priorities.

## 🚀 Quick Start

### 1. Setup
```powershell
python -m venv .venv
.\.venv\Scripts\Activate
pip install -r requirements.txt
```

### 2. Running
```bash
python main.py
```

### 3. Output
Response plans are generated in `plans/` as JSON/YAML files.
