# PredictPath AI - Tool 6: Governance & Reporting

## 📋 Overview
Tool 6 is the governance layer. It ensures all actions taken by the creation system comply with defined policies and generates final audit reports.

## 🚀 Quick Start

### 1. Setup
```powershell
python -m venv .venv
.\.venv\Scripts\Activate
pip install -r requirements.txt
```

### 2. Running
```bash
python main.py
```

### 3. Database
This tool uses a local SQLite database (`data/governance.db`) which is **git-ignored**. It will be created on first run.
